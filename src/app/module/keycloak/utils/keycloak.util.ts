/**
 * Classe utilitaire pour les opérations liées à Keycloak.
 * Destinée à regrouper des fonctions statiques d'aide
 * (ex: extraction de claims du token, formatage de rôles, helpers réutilisables
 * hors du contexte Angular/injection).
 *
 * ✅ Vide pour le moment — aucun impact sur l'init Keycloak ni sur les guards.
 */
export class KeycloakUtil {}
